# Lightbox Web

Frontend-only temporary deployment for Lightbox.

The catalog and resolver remain on the owner's Mac and are reached through a temporary HTTPS tunnel. The site is available only while both the local Lightbox service and tunnel are running.
